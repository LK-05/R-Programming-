#1.6 Without using a vector, calculate the sum of the same five student scores. 
#Calculate the average of the scores without using the mean function. Display the sum and average calculated directly.
# Calculating the sum of student scores without using a vector sum_of_scores  =  85 + 92 + 78 + 95 + 88
sum_of_scores
# Calculating the average  without using the mean function average_without_vector =  sum_of_scores / 5
average_without_vector