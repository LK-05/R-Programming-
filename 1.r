#1.1 Create a numeric vector named "grades"  containing the scores of five students: 85, 92, 78, 95, and 88. Calculate the average (mean) of the grades using the appropriate function. Display the contents of the 
"grades" vector.
# Creating a numeric vector named "grades"
grades <- c(85, 92, 78, 95,  88)
# Calculating the average (mean) of grades
average_grade <- mean(grades)
average_grade
# Displaying the contents of the "grades" vector
grades
# Accessing the third grade using indexing
third_grade <- grades[3]
third_grade